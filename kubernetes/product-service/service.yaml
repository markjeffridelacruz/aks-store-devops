apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: aks-store
  labels:
    app: product-service
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
  selector:
    app: product-service
