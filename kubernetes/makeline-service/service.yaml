apiVersion: v1
kind: Service
metadata:
  name: makeline-service
  namespace: aks-store
  labels:
    app: makeline-service
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
  selector:
    app: makeline-service
