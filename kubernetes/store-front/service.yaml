apiVersion: v1
kind: Service
metadata:
  name: store-front
  namespace: aks-store
  labels:
    app: store-front
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: store-front
