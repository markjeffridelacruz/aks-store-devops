apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: aks-store
  labels:
    app: ai-service
spec:
  type: ClusterIP
  ports:
    - port: 5001
      targetPort: 5001
  selector:
    app: ai-service
